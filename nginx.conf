# Redirect cherry-ai.com apex to www
server {
    listen 80;
    server_name cherry-ai.com;
    return 301 https://www.cherry-ai.com$request_uri;
}

# Redirect www.cherryai.com.cn to cherryai.com.cn
server {
    listen 80;
    server_name www.cherryai.com.cn;
    return 301 https://cherryai.com.cn$request_uri;
}

# Main server
server {
    listen 80;
    server_name www.cherry-ai.com cherryai.com.cn;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml+rss image/svg+xml;

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback - handle client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
